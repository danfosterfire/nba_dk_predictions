torch>=2.2.0
numpy>=1.26.0
pandas>=2.2.0
scikit-learn>=1.4.0
nba_api>=1.4.1
pyyaml>=6.0
tqdm>=4.66.0
xgboost>=2.0.0
matplotlib>=3.8.0
seaborn>=0.13.0
plotly>=5.20.0
streamlit>=1.33.0
pyarrow>=15.0.0
jupyter>=1.0.0
pytest>=8.0.0
requests>=2.31.0
beautifulsoup4>=4.12.0
pypdf>=6.0.0        # NBA official injury-report PDFs (src/data/injury_reports.py)
# The Stan heads (src/stan/*.stan, src/models/stan_*.py). cmdstanpy is only the Python
# wrapper — CmdStan itself is a separate ~1 GB toolchain install and is NOT pip-managed:
#   .venv/bin/python -c "import cmdstanpy; cmdstanpy.install_cmdstan()"
cmdstanpy>=1.3.0
