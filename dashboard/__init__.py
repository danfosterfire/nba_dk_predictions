"""The project walkthrough — nine tabs over the artifacts the pipeline produced.

`dashboard/README.md` carries the precedence rule: `CLAUDE.md` and the
`docs/*-plan.md` files are the source of truth, and `decisions.py` is a
distillation of them for browsing.
"""
